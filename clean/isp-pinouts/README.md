# Clean ISP pinouts

This folder is intentionally reserved for ISP derivatives that pass detail-preservation QA.

The original `isp-pinouts/` collection remains untouched. EasyJTAG-style diagonal overlays frequently cross traces, pads, and chip detail, so automated derivatives are not admitted here until the cleanup method preserves those technical features reliably.
